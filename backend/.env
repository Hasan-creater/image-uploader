PORT=5000
MONGO_URI=mongodb://localhost:27017/image-uploader
JWT_SECRET=yoursecretkey123
IMAGE_ENCRYPTION_KEY=12345678901234567890123456789012


Login → get token → store token → open dashboard → upload image → backend saves file →
-> database saves info → dashboard fetch images → images displayed.